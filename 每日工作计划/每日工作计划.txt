************************重要备忘*************
公司电脑屏幕坏了，不好移动，只能放在集控室，但很方便用来连接现场环境服务器
所有主要以自己电脑上的为最终版本，其次才是辅料库开发服务器45上的为最终版本。
************************重要备忘*************

》Git最新版本
2021年7月2日16:35:48，服务器电脑上的sqlserver数据库是最新的。

>>>> 如下功能先缓缓，后面尽快抽时间完成的功能
1）堆垛机除非处于自动模式，如果是其他模式，比如维修模式、自动模式，都要把堆垛机本体电控柜上的调整按钮打到右边。
1）。。。查看动软源代码？？
2）思维导图，内含一级、二级、三级。每级都能存放文字和图片，每级的文字也可以分级别，每级别也能够被下级文字引用。
SynCompany的字符串工具添加字符串时候，可以带有格式，类似这样的语句：Append(string.Format({0},1));
FileSave获取所有软件安装包所有相关内容，如果含有XXX.ZIP文件，那么某一目录下有文件夹XXX，那么就不用包含这个XXX文件以及其子目录下所有的目录和文件；
SynCompany的字符串工具，功能：从更改历史记录中选择一组直接执行，也可以选中组的某一动作来执行。
	1、显示出所有历史动作
	2、勾选某些动作
	3、对Input字符串执行这些勾选的动作得到结果。	
		*****就这步难。我已经保存了动作名称，保存了参数。额，Switch(动作名称){case 动作名称01:某某函数.(参数1，参数2，...参数N);break;case 02:...; break;

>>>>时间，还得观察的情况
1、申请出库几托盘，利用OutBillService的ApplyOutOnePallet函数
1、上位发送任务给AGV，agv会反馈任务ERROR，再发一次之后还是，直到黄康删除任务才正常
1、ice中通过条码找相应盘点明细。
1、盘点流程中如果找不到扫描条码的话，则选择好站台后，Android通过站台号发送给后台，后台找已经到达此站台的任务，然后从任务中获取条码和货位位置，这样得到的理论上是这个托盘对应的系统条码
1、测试build功能，把release文件夹下的ice拷贝到D盘，然后mklink，而后build，看看这样的话，是否能build后，把新增的文件不显示在git中。还是使用172.30.10.22和199.199.180.6吧
1、同步，本地添加Product中的产品简码，不用了。直接使用备注就好。
海林区号是0453

>>>> 后续要完成功能：
1、出库端LED开始设置成功172.30.20.81后，想改成172.30.20.82，导致现在根本用不了了，先弄对接MES吧
1、先把表面工作做完，比如LED的显示，然后再谈其他有什么优化的，不然客户老觉得你没完成。
1、在服务器上测试和MES对接的WebService。
1、要料统计报表预留每个牌号8个机组的要料统计，如果这个牌号只有五个机组的统计，那先三个全部为0，后期如果需要优化为那三个不显示，再优化就是
1、机组退空托盘的时候，工单明明都是乙班的，但在机组查询那里会出现是甲班的退空托盘
2、00030000这个是什么报警编码？堆垛机2021年3月18日我测试时经常报这个错误。处理结果：还是先把成品库比较完整的堆垛机报警复制到辅料库
3、正常情况下是应该先发送。处理结果：什么鬼，先发送什么？？？

》 后期要实现的扩展功能
1）早点实现功能：将左侧文本框中的所有列表中文件复制到右侧对应Index的文件中。
1）不用再写动软模板了，使用亚金推荐的T4模板，已经有现成的批量生成功能代码了，不过就是限制在sqlite了。
	这种数据库它缺少：1、生成数据库中表单；2、无法读取到数据库表中的列注释；
	感觉还是难，生成sqlite中数据库表单的sql是可以通过最开始的行索引字符串数组来生成，但针对问题如果这个行字符串需要注释，这lotter代码没实现，查找资料后发现sqlite只通过读表中数据来保存并读取了。
	==>还是得改进Lottoery的T4模板代码，要从数据表中读取内容。     不过先已经有的代码吧
1）不同的数据库，你将Excel转换成某一数据库语句，还不如1、转换为某些类；2、某些类映射成数据库。这种方式还很通用
6)抽时间了解DialogResult的什么时候起作用。
7)ice配置文件中将192.168.137.1改成localhost会是的ice得到真正结果吗？
10）能否从一个变量类的引申出都有哪些类调用过它，都是怎么个顺序调用的？有了这个之后，就可以从一个地方推出都是哪里用过了。以后改代码就超级好改了。
	其实VS已经通过查看引用就可以查看哪些调用类，但又要通过查看引用才能找得出类中函数又调用了哪些类的函数，或者又要通过查看引用查看哪些类使用了这个函数。
11）replaceStr中能否使用$的功能？？？？如可以将某些内容替换为$("{DateTime.Now}")

NNN）这个太难了。还是后面再说吧。在想将oracle的sql语句转换成sqlserver的sql时候碰到日期类型，一下不知道如何转换了。
