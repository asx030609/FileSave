************************重要备忘*************
公司电脑屏幕坏了，不好移动，只能放在集控室，但很方便用来连接现场环境服务器
所有主要以自己电脑上的为最终版本，其次才是辅料库开发服务器45上的为最终版本。
************************重要备忘*************

》Git最新版本
2020年8月21日10:32:49，相对洛阳辅料库来说，45上的是最新的，但都已经上传到公司服务器了

>>>> 后面尽快抽时间完成的功能
git获取某个版本的前的内容：直接命令是没有的，但可以先提交本地为提交的修改，好以后cherry pick出来1、创建一个temp分支2、checkout这个分支3、回退到这个版本4、丢弃所做的修改5、使用git diff --name得到改动的文件6复制相应文件呗
SynCompany的字符串工具添加字符串时候，可以带有格式，类似这样的语句：Append(string.Format({0},1));
FileSave获取所有软件安装包所有相关内容，如果含有XXX.ZIP文件，那么某一目录下有文件夹XXX，那么就不用包含这个XXX文件以及其子目录下所有的目录和文件；
SynCompany的字符串工具，功能：“每行中这些字符后的内容都删除”，是否可以考虑前几个或者后几个，不然遇到AXXBXXCXXDEFGH，想删除第二个XX之前的内容都无法删除。（特殊情况，但总会出现的）
SynCompany的字符串工具，功能：从更改历史记录中选择一组直接执行，也可以选中组的某一动作来执行。

>>>> 2020年12月22日16:47:39，还得观察的情况
1）MES会下达 喂丝机，辅料库那里要过滤过。
2）修正机组呼叫可以要料到不同机组或不同班组退回的余料
3）机组下班如果碰到MES错误会造成生成两条移交单据BUG，已改正
4）调整机组消耗统计给MES，整合成一个机组一个xml发送

>>>> 打算后续完成功能：
1）盘点回库在出两拖盘的时候，现在只有在生成堆垛机的设备任务时会去检查下两位置容量是否已满，但现在最大任务数为2，在盘点点击作业后，会一下生成两设备任务。
	没用，第一托到达盘点站台后，是会任务到达，会完成的，会把入库端堆垛机放货口的容量位置清除为0，然后就可以生成堆垛机任务了。
在电控控制只有一拖可到达盘点站台后，再次进行测试发现：
    2020年12月22日14:47:50，
    1-30-01层的商标纸托盘条码好像有点问题，内外要调换。03790045是先入库了。最终库存是对得上。
    那么再盘点1-30-01和其他
    0523先出来（实物对的上）。然后就会先入进去了。但任务号对应的是42815，这任务的库存顺序是1（这和实际不匹配），对应条码应该是 03790045，是最开始下发给电控的任务到达就是错误的吗？应该是了。
    然后，这次结单所有盘点单据后0523的库存顺序是2，这库存和实物又对不上了。重新盘点一次吧
    2020年12月22日16:06:17，
    开始再对1-30-01进行盘点
    0045先进去。结单后，最终的库存又和实物对的上了。托盘条码对应的库存顺序也对得上了。

1）双深位盘点回库 库存回库时，库存顺序会变动，如果刚好是余料返库的盘位和正常入库无标识的盘位各占一盘，那么顺序就不能反掉。除非
	1、扫码入库时候，优先无余料返库的货位，如果没其他空余货位时，提示打开某参数。但这种情况下，在盘点时要注意是否双伸位上有余料返库和正常入库物料共存。
	2、盘点回库到高架库的时候，先放到另外一个空货位，然后锁定这货位，待另一盘入库后，再把这盘回库到原货位。
	3、或者像亚金说的，在要料时，不论一个货位的库存顺序如何变动，都只能要本班本机组退回的物料。
		具体代码来看，就是带身份标识去查找时候，发现没有，但不带标识去查找，能发现有Location。再具体点，就是只找外面那个盘位是否满足条件。
1）库存时间是否在机组退回时会变动？如果会变动，那很有可能是这个原因造成有余料返库的物料要不走。
2）人领人退，要把托盘类型和机组对应起来。比如卷烟机组下只有卷接盘可弹出供选择
3）机组消耗统计报表需要依据的序号的可改操作，要对这些表添加字段。只改了产品信息的字段
）27013任务号，1#堆垛机等了23秒才启动，准备取货。27043用了13秒
》 后期要实现的扩展功能
。。。查看动软源代码？？
6)抽时间了解DialogResult的什么时候起作用。
7)ice配置文件中将192.168.137.1改成localhost会是的ice得到真正结果吗？
8)自己电脑smartGit执行Fetch(也可能是执行其他功能)，同时运行VS的话，两个一起卡住。要想办法解决。
很有可能是git后台拉取卡死的问题，2020年8月1日10:16:43，强制关闭smartGit后，就不会一起卡住了。
9）2020-7-25号17点之后，换牌后，说有余料返库，但报表统计那里机退全部为0，哪里有异常？？？
10）能否从一个变量类的引申出都有哪些类调用过它，都是怎么个顺序调用的？有了这个之后，就可以从一个地方推出都是哪里用过了。以后改代码就超级好改了。
	其实VS已经通过查看引用就可以查看哪些调用类，但又要通过查看引用才能找得出类中函数又调用了哪些类的函数，或者又要通过查看引用查看哪些类使用了这个函数。
11）replaceStr中能否使用$的功能？？？？如可以将某些内容替换为$("{DateTime.Now}")
）早点实现功能：将左侧文本框中的所有列表中文件复制到右侧对应Index的文件中。
